export class AppConstants {
  static readonly baseUrl: string = 'https://api.opendota.com';
  static readonly imageBaseUrl: string =
    'https://cdn.cloudflare.steamstatic.com';

  static readonly keyFavoriteIds: string = 'keyFavoriteIds';
}
